# Copy this to c2b.env for the local registerC2BUrls.js script ONLY.
# Never commit the real c2b.env file — it's already git-ignored.
#
# The deployed Cloud Functions themselves read these as Secret Manager
# secrets, set with `firebase functions:secrets:set NAME` (see SETUP-MPESA.md),
# not from this file — this file just lets registerC2BUrls.js run locally.
#
# Deliberately NOT named ".env" — Firebase's own `firebase deploy` auto-loads
# any file named exactly ".env" (or ".env.<project-id>") in this folder as
# real Cloud Functions environment variables, which then collides with the
# secrets of the same name and breaks deployment. Keep it as "c2b.env".

# From developer.safaricom.co.ke -> My Apps -> your app
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=

# Your Till/Paybill number. Use 174379 for Safaricom's shared sandbox
# Paybill while testing, then your real number once you're on production.
MPESA_SHORTCODE=

# From the same Daraja app page (Lipa Na M-Pesa Online passkey).
# Safaricom publishes a shared sandbox passkey in their docs for testing.
MPESA_PASSKEY=

# "till" for a Buy Goods Till number, "paybill" for a Paybill number.
# Safaricom rejects STK Push requests that use the wrong one for your
# shortcode, so this has to match what you actually have.
MPESA_ACCOUNT_TYPE=till

# "sandbox" while testing, "production" once your Till is live and you've
# been approved for the production Daraja app.
MPESA_ENV=sandbox

# The base URL Firebase gives you after `firebase deploy --only functions`,
# e.g. https://us-central1-kenokip-farm.cloudfunctions.net
MPESA_CALLBACK_BASE_URL=

# The SAME value you set with:
#   firebase functions:secrets:set MPESA_WEBHOOK_SECRET
# (any long random string of your choosing — see SETUP-SECURITY.md). Putting
# it here too lets this script register your C2B URLs WITH that secret
# attached, e.g. .../c2bConfirmation?key=<this value>, so a stranger who
# finds these URLs on their own can't feed them fake payment data. Leave
# blank only if you haven't set that secret yet — everything still works,
# just without that extra protection until you do both steps.
MPESA_WEBHOOK_SECRET=
