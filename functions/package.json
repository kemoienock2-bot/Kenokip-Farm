{
  "name": "kenokip-farm-functions",
  "description": "M-Pesa (Safaricom Daraja) backend for the Kenokip Farm Finance section — STK Push deposits initiated by the owner, and C2B webhooks for automatic deposit detection when someone else pays the till.",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "register-c2b": "node registerC2BUrls.js",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "node-fetch": "^2.7.0"
  },
  "private": true
}
